---
/**
 * Fusszeile der Vorlage: Lizenzhinweis links, drei Verweise rechts.
 *
 * Steht in PageFrame.astro als letztes Kind von .main-frame und liegt damit
 * neben der Seitenleiste, nicht darunter. Genau so ist es in der Vorlage
 * aufgebaut.
 *
 * Nicht zu verwechseln mit Footer.astro: Das ist Starlights Artikelfuss und
 * traegt Autorenblock und Angebotsverweis am Ende eines Beitrags.
 *
 * ACHTUNG, noch zu bestaetigen: Der Lizenzsatz und die Adressen von Impressum
 * und Datenschutz stammen aus der Designvorlage beziehungsweise sind
 * angenommen. Im Repo gab es dazu bisher keine Angabe.
 */

const links = [
  { label: 'Zur Beratungsseite', href: 'https://wissen-und-werkzeug.de/' },
  { label: 'Impressum', href: 'https://wissen-und-werkzeug.de/impressum/' },
  { label: 'Datenschutz', href: 'https://wissen-und-werkzeug.de/datenschutz/' },
];
---

<footer class="seitenfuss">
  <div class="seitenfuss-inhalt">
    <span>Wissen &amp; Werkzeug Wiki · Inhalte unter CC BY-SA 4.0</span>
    <div class="seitenfuss-links">
      {links.map((link) => <a href={link.href}>{link.label}</a>)}
    </div>
  </div>
</footer>

<style>
  .seitenfuss {
    padding: 36px var(--sl-content-pad-x) 56px;
    border-top: 1px solid var(--w-border);
    background: var(--w-side);
  }

  /* Dieselbe Inhaltsbreite wie die Startseite der Vorlage, damit Fusszeile und
     Inhalt darueber an derselben Kante stehen. */
  .seitenfuss-inhalt {
    max-width: 1000px;
    margin: 0 auto;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    font-size: 0.90625rem; /* 14.5px */
    color: var(--w-muted);
  }

  .seitenfuss-links {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
  }

  .seitenfuss-links a {
    color: var(--w-accent);
    text-decoration: none;
  }

  .seitenfuss-links a:hover,
  .seitenfuss-links a:focus-visible {
    text-decoration: underline;
  }
</style>
