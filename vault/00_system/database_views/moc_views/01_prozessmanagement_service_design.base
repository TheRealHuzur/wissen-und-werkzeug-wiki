views:
  - type: table
    name: Tabelle
    filters:
      and:
        - parent_topic == "prozessmanagement"
        - subtopic == "service_design"
        - "!type.isEmpty()"
    order:
      - file.name
      - status
