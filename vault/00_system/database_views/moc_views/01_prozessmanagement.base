views:
  - type: table
    name: Tabelle
    filters:
      and:
        - parent_topic == "prozessmanagement"
        - "!type.isEmpty()"
    order:
      - file.name
      - status
    sort:
      - property: status
        direction: DESC
