views:
  - type: table
    name: Tabelle
    filters:
      and:
        - ebene_1 == "prozessmanagement"
        - file.folder == "20_ip_atoms"
    order:
      - file.name
      - status
    sort:
      - property: status
        direction: DESC
