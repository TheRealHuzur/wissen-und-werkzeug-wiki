views:
  - type: table
    name: Tabelle
    filters:
      and:
        - parent_topic == "prozessmanagement"
        - subtopic == "controlling"
        - "!type.isEmpty()"
    order:
      - file.name
      - status
