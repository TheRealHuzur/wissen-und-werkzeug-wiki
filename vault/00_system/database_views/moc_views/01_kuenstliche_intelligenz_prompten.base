views:
  - type: table
    name: Tabelle
    filters:
      and:
        - parent_topic == "kuenstliche_intelligenz"
        - subtopic == "prompten"
        - "!type.isEmpty()"
    order:
      - file.name
      - status
